<h3>All Users</h3>

<h2>
  <ul><% @users.each do |user| %>
    <li>
      <%= user.id %>.
      <%= user.fname %>
      <%= user.lname %>
    </li>
    <li>
      <%= user.username %>
    </li>
    <li>
      <%= user.email %>
    </li>

    <br><br>

    <%= link_to ("User's Profile page"), user_path(user.id) %> |
    <%= link_to 'Delete user',  { action: :destroy, id: user.id }, method: :delete %>
    <hr>

    <% end %>
  </ul>
</h2>
